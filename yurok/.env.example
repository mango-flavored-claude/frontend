# Leave empty when the API is served from the same domain as the frontend.
# Do not add a trailing slash.
VITE_API_URL=

# URL path where the built app will be mounted.
# Use / for a domain root, or for example /frontend/ for GitHub Pages.
VITE_BASE_PATH=/
